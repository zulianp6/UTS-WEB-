let string = "SCOOTER"


for (let i = 0; i < string.length; i++) {
    const vocal = string[i].substring(0,1);

    if (vocal == 'C') {
        let test = string.indexOf('C')
        string[test] = 'Z'
    }
}